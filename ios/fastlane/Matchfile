# Match configuration for CI (HTTPS + PAT)
git_url("https://github.com/bitlogiq/ios-cert-store.git")
git_branch("main")

type("appstore")
readonly(true)
storage_mode("git")

app_identifier([ENV["APP_IDENTIFIER"]])
username(ENV["APPLE_ID"])
team_id(ENV["TEAM_ID"])
