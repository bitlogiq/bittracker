git_url("https://x-access-token:${MATCH_GIT_PAT}@github.com/bitlogiq/ios-cert-store.git")
git_branch("main")

type("appstore")

app_identifier(["za.co.bitlogiq.bittracker"])
username("roelofhorowitz@hotmail.com")
team_id("R4R4AW7JUQ")

keychain_name("login.keychain-db")
keychain_password(ENV["MATCH_PASSWORD"])
