git_url(ENV["MATCH_GIT_URL"])   # e.g. git@github.com:bitlogiq/ios-cert-store.git (SSH)
git_branch("master")            # keep "master" since your log shows master
type("appstore")
readonly(true)
storage_mode("git")
