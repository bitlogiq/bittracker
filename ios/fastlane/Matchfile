git_url("https://github.com/bitlogiq/ios-cert-store.git") # ✅ HTTPS, not SSH
git_branch("main")  # or "master", depending on the cert repo
type("appstore")
readonly(true)

app_identifier(["za.co.bitlogiq.bittracker"])
username("roelofhorowitz@hotmail.com")
team_id("R4R4AW7JUQ")
