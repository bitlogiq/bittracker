git_url("https://github.com/bitlogiq/ios-cert-store.git")
git_branch("main")

type("appstore")
storage_mode("git")

app_identifier(["za.co.bitlogiq.bittracker"])
username("roelofhorowitz@hotmail.com")
team_id("R4R4AW7JUQ")

# On CI we pass Basic auth via env to fastlane 'match' directly
readonly(true)
